import type { PreviewContent } from '../../types/sectionContent'

export type { PreviewContent, ContentItem } from '../../types/sectionContent'

export interface SectionPreviewProps {
    content: PreviewContent
    m: boolean
}
